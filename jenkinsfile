pipeline {
    agent { label 'slave-1'}
    tools {
        terraform 'terraform-11'
    }
    stages {
        stage("Git checkout"){
            steps{
                git branch: 'main', url: 'https://github.com/DevOps-team-1/terraform'
            }
        }
        stage("Terraform init"){
            steps{
                sh 'terraform init'
            }
        }
        stage("Terraform apply"){
            steps{
                sh 'ls'
                sh 'terraform apply --auto-approve'
            }
        }
    }
}
